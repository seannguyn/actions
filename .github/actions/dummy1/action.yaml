name: Build and deploy serverless application on Kubernetes

runs:
  using: "composite"
  steps:
    - name: dummy1
      shell: bash
      run: |
        echo "dummy1"
        sleep 2s
        pwd
        ls
